# ROS Node to Downsample OccupancyGrids

This is a simple package to downsample an occuapancy grid of one resolution (cell/m) into a lower resolution. 

# Basic Usage

```bash
roslaunch map_downsample map_downsample.launch
```
